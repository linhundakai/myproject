
server:
  port: 8081
  
spring:
  application:
    name: service0
  cloud:
    config:
      uri: http://localhost:8084
      name: service0
      profile: native
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/  #注册中心地址
  instance:
    hostname: localhost
    instance-id: http://localhost:8081

